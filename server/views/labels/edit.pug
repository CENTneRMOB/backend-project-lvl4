extends ../layouts/application2.pug

block header
  = t('views.labels.exist.editing')

block content
  +formFor()(action=route('patchLabel', { id: label.id }) method='post')
    +input(label, 'name', errors, 'labels')(type="text")
    input(name='_method' type='hidden' value='patch')
    +submit(label)(value=t('views.labels.exist.edit'))
